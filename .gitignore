# Python virtual environment
venv/
env/
__pycache__/
*.pyc

# Node.js (if report.js/package.json turn out to be unrelated to this project,
# consider removing them from the repo entirely rather than just ignoring them)
node_modules/

# Streamlit secrets (never commit API keys/tokens)
.streamlit/secrets.toml

# OS / editor cruft
.DS_Store
.vscode/
.idea/
.ipynb_checkpoints/
